/**
 * Tabs on the home screen.
 */
export enum Tab {
    Menu,
    Build,
    User,
}

/**
 * Tools on the build tab.
 */
export enum Tool {
    None,
    Brush,
    Eraser,
}

/**
 * Maze lists on the user tab.
 */
export enum List {
    MyMazes,
    Favorites,
}

/**
 * Information contents.
 */
export enum Info {
    TermsOfUse,
    PrivacyPolicy,
}
